"""Session export — the archive's canonical Session/Message/Part model rendered back out
as a file (HTML, Markdown) rather than into the DB. See docs/export-requests.md for the
opposite-direction feature (pulling a provider's own export in); this is the other way.
"""
